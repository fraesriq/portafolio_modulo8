<nav id="secundario" class="navbar navbar-expand-lg navbar-dark bg-body-tertiary">
  <div class="container">
    <a class="navbar-brand" href="/">MERCADO</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Principal</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#carousel">Nosotros</a>
        </li>
        <li class="nav-item" id="linkProd">
          <a class="nav-link"id="btn_prod" href="#">Productos</a>
        </li>
        <li class="nav-item" id="linkMant">
          <a class="nav-link" id="btn_mant" href="#">Mantenedor</a>
        </li>
        <li class="nav-item" id="linkSails">
          <a class="nav-link" id="btn_sails" href="#">Ventas</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/contacto">Contacto</a>
        </li>
        <li class="nav-item" id="linkCar">
          <a class="nav-link"id="btn_car" href="#"><i class="fa-solid fa-cart-shopping" style="font-size: 1.5em;"></i></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" id="amount-products"></a>
        </li>
        <li class="nav-item" id="linkLogin">
          <a class="nav-link" href="/login"><i class="fa fa-user"></i> Login</a>
        </li>
        <li class="nav-item" id="linkLogout" style="display: none;">
          <a class="nav-link" id="btnLogout" href="#"><i class="fa fa-arrow-right"></i> Cerrar Session</a>
        </li>
        <li class="nav-item" id="linkRegister">
          <a class="nav-link" href="/registro">|  Registrar</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<script>
  let token = localStorage.getItem('jwt');

  let linkLogin = document.getElementById("linkLogin");
  let linkLogout = document.getElementById("linkLogout");
  //let linkRegister = document.getElementById("linkRegister");

  if (token) {
    linkLogout.style.display = "block";
    linkRegister.style.display = "none";
    linkLogin.style.display = "none";
  } else {    
    linkLogout.style.display = "none";
    linkProd.style.display = "none";
    linkMant.style.display = "none";
    linkSails.style.display = "none";
    linkCar.style.display = "none";
  }  

  btn_prod.addEventListener("click", (event) => {
    event.preventDefault();
    if (token) {
      location.href = "/productos?token=" + token
    } else {
      toastr.error("Debe loguearse para obtener permisos a esa ruta.");
    }
  })

  btnLogout.addEventListener("click", (event) => {
    localStorage.clear();
    location.href = "/";
  })
  
  btn_mant.addEventListener("click", (event) => {
    event.preventDefault();
    if (token) {
      location.href = "/mantenedor?token=" + token
    } else {
      toastr.error("Debe loguearse para obtener permisos a esa ruta.");
    }
  })
  
  btn_sails.addEventListener("click", (event) => {
    event.preventDefault();
    if (token) {
      location.href = "/ventas?token=" + token
    } else {
      toastr.error("Debe loguearse para obtener permisos a esa ruta.");
    }
  })
  
  btn_car.addEventListener("click", (event) => {
    event.preventDefault();
    if (token) {
      location.href = "/carro?token=" + token
    } else {
      toastr.error("Debe loguearse para obtener permisos a esa ruta.");
    }
  })

</script>
